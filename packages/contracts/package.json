{
  "name": "@gamja/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:unit": "vitest run",
    "test:contract": "vitest run test/openapi.spec.ts"
  },
  "devDependencies": {
    "@gamja/config": "workspace:*",
    "@types/node": "^24.0.0",
    "eslint": "^9.30.0",
    "typescript": "^6.0.0-dev.20260715",
    "vitest": "^3.2.0"
  }
}
